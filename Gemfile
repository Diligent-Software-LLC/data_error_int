source 'https://rubygems.org'

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in data_error_int.gemspec
gemspec

gem 'minitest', '~> 5.0'
gem 'rake', '~> 13.0'
